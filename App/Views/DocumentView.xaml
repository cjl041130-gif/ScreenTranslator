<UserControl x:Class="ScreenTranslator.Views.DocumentView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ScreenTranslator"
 AllowDrop="True" DragEnter="OnDragEnter" DragLeave="OnDragLeave" Drop="OnDrop">
 <UserControl.Resources><local:DocumentPreviewVisibilityConverter x:Key="DocumentPreviewVisibility"/></UserControl.Resources>
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Grid Margin="0,0,0,10" Visibility="{Binding IsEmpty,Converter={StaticResource BoolVisibility}}">
   <StackPanel><TextBlock Text="文档翻译" Style="{StaticResource PageTitle}"/><TextBlock Text="导入 PDF、PPTX 或 DOCX，在本机解析并使用 AI 翻译文字" Style="{StaticResource BodyMuted}"/></StackPanel>
   <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="{StaticResource SuccessSoft}" CornerRadius="13" Padding="12,7"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE73E;" Style="{StaticResource Icon}" Foreground="{StaticResource Success}" FontSize="14" Margin="0,0,7,0"/><TextBlock Text="文档在本机解析" Foreground="{StaticResource Success}"/></StackPanel></Border>
  </Grid>

  <Grid Grid.Row="1" Visibility="{Binding IsEmpty,Converter={StaticResource BoolVisibility}}">
   <Border x:Name="DropZone" Style="{StaticResource CardStyle}" BorderThickness="2" Padding="40" AutomationProperties.AutomationId="DocumentDropZone">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="560">
     <Border Width="78" Height="78" CornerRadius="39" Background="{StaticResource AccentSoft}"><TextBlock Text="&#xE8A5;" Style="{StaticResource Icon}" Foreground="{StaticResource Accent}" FontSize="34" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
     <TextBlock Text="拖拽 PDF、PPTX 或 DOCX 到这里" FontSize="23" FontWeight="SemiBold" TextAlignment="Center" Margin="0,24,0,8"/>
     <TextBlock Text="或点击选择文件" Style="{StaticResource BodyMuted}" TextAlignment="Center"/>
     <Button Content="选择文件" Command="{Binding SelectFileCommand}" Style="{StaticResource PrimaryButton}" Width="176" Margin="0,22,0,18" AutomationProperties.AutomationId="SelectDocument"/>
      <TextBlock Text="PDF、PPTX、DOCX · 单个文件最大 200 MB" Style="{StaticResource Caption}" TextAlignment="Center"/>
      <Border Background="{StaticResource ErrorSoft}" CornerRadius="10" Padding="12" Margin="0,12,0,0" Visibility="{Binding HasError,Converter={StaticResource BoolVisibility}}"><TextBlock Text="{Binding Error}" Foreground="{StaticResource Error}" TextAlignment="Center"/></Border>
     <Border Background="{StaticResource Canvas}" CornerRadius="10" Padding="16" Margin="0,24,0,0"><StackPanel>
      <TextBlock Text="✓ PDF / PPTX / DOCX 在本机解析" Foreground="{StaticResource Success}" Margin="0,0,0,5"/>
      <TextBlock Text="✓ 扫描 PDF 使用本地 OCR" Foreground="{StaticResource Success}" Margin="0,0,0,5"/>
      <TextBlock Text="✓ 只向翻译服务发送提取后的文字" Foreground="{StaticResource Success}" Margin="0,0,0,5"/>
      <TextBlock Text="✓ 不上传文档、截图或页面图片，原文件不会被修改" Foreground="{StaticResource Success}"/>
     </StackPanel></Border>
    </StackPanel>
   </Border>
  </Grid>

  <Grid Grid.Row="1" Visibility="{Binding HasDocument,Converter={StaticResource BoolVisibility}}">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <Border Style="{StaticResource CardStyle}" Padding="7,6" Margin="0,0,0,5">
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <StackPanel VerticalAlignment="Center" Margin="0,0,8,0">
      <TextBlock Text="{Binding FileName}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding FileName}"/>
      <StackPanel Orientation="Horizontal" Margin="0,2,0,0"><TextBlock Text="{Binding FileType}" Style="{StaticResource Caption}"/><TextBlock Text=" · " Style="{StaticResource Caption}"/><TextBlock Text="{Binding PageCount}" Style="{StaticResource Caption}"/><TextBlock Text=" 页 · " Style="{StaticResource Caption}"/><TextBlock Text="{Binding BlockCount}" Style="{StaticResource Caption}"/><TextBlock Text=" 段 · " Style="{StaticResource Caption}"/><TextBlock Text="{Binding StateText}" Foreground="{StaticResource Accent}" ToolTip="{Binding Status}"/></StackPanel>
     </StackPanel>
     <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
      <Button Content="打开原文件" Command="{Binding OpenOriginalCommand}" Margin="0,0,5,0" Padding="7,4" AutomationProperties.AutomationId="OpenOriginalDocument"/>
      <Button Content="开始翻译" Command="{Binding StartTranslationCommand}" Style="{StaticResource PrimaryButton}" MinWidth="84" Padding="8,5" AutomationProperties.AutomationId="StartDocumentTranslation"/>
     </StackPanel>
    </Grid>
   </Border>

   <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border Width="130" Style="{StaticResource CardStyle}" Padding="10" Margin="0,0,10,0" Visibility="{Binding ShowPageList,Converter={StaticResource BoolVisibility}}"><DockPanel><TextBlock Text="页面" Style="{StaticResource SectionTitle}" Margin="8,5,0,12" DockPanel.Dock="Top"/><ListBox ItemsSource="{Binding Document.Pages}" SelectedItem="{Binding SelectedPage}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled"><ListBox.ItemTemplate><DataTemplate><Border Background="{StaticResource Canvas}" CornerRadius="8" Padding="10" Margin="2,3"><StackPanel><TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/><StackPanel Orientation="Horizontal" Margin="0,5,0,0"><TextBlock Text="{Binding TextBlocks.Count}" Style="{StaticResource Caption}"/><TextBlock Text=" 个文字块" Style="{StaticResource Caption}"/></StackPanel></StackPanel></Border></DataTemplate></ListBox.ItemTemplate></ListBox></DockPanel></Border>

    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="0" Margin="0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="7,3"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="页面预览" ToolTip="PDF 使用原始页面；PPTX 和 DOCX 根据本机解析出的文本位置重建。复杂图像和版式可通过“打开原文件”查看。" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/><ComboBox Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Width="100" Padding="5,2" Margin="0,0,5,0" ItemsSource="{Binding PdfHalfNames}" SelectedItem="{Binding SelectedPdfHalfName}" Visibility="{Binding IsPdfDocument,Converter={StaticResource BoolVisibility}}"/><ComboBox Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Width="96" Padding="5,2" ItemsSource="{Binding PreviewModeNames}" SelectedItem="{Binding SelectedPreviewModeName}"/></Grid></Border>
     <Grid Grid.Row="1" Background="{StaticResource PreviewCanvas}">
      <Grid Visibility="{Binding IsPdfDocument,Converter={StaticResource BoolVisibility}}">
       <Viewbox Stretch="Uniform" Margin="0">
        <Grid Width="{Binding SelectedPage.Width}" Height="{Binding PdfHalfHeight}" ClipToBounds="True">
         <Canvas Width="{Binding SelectedPage.Width}" Height="{Binding SelectedPage.Height}" ClipToBounds="True" HorizontalAlignment="Left" VerticalAlignment="Top" RenderTransform="{Binding PdfHalfTransform}">
          <Image Source="{Binding CurrentPreviewImage}" Width="{Binding SelectedPage.Width}" Height="{Binding SelectedPage.Height}" Stretch="Fill"/>
          <ItemsControl ItemsSource="{Binding CurrentBlocks}" Visibility="{Binding ShowChinese,Converter={StaticResource BoolVisibility}}">
           <ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
           <ItemsControl.ItemContainerStyle><Style TargetType="ContentPresenter"><Setter Property="Canvas.Left" Value="{Binding Bounds.X}"/><Setter Property="Canvas.Top" Value="{Binding Bounds.Y}"/></Style></ItemsControl.ItemContainerStyle>
           <ItemsControl.ItemTemplate><DataTemplate><Border Width="{Binding Bounds.Width}" Height="{Binding Bounds.Height}" Background="White" Padding="1" ClipToBounds="True" Visibility="{Binding HasTranslation,Converter={StaticResource BoolVisibility}}"><TextBlock Text="{Binding TranslatedText}" FontSize="{Binding OverlayFontSize}" Foreground="{StaticResource Text}" FontWeight="{Binding FontWeight}" TextWrapping="Wrap" VerticalAlignment="Center"/></Border></DataTemplate></ItemsControl.ItemTemplate>
          </ItemsControl>
         </Canvas>
        </Grid>
       </Viewbox>
      </Grid>
      <Viewbox Stretch="Uniform" Margin="4" Visibility="{Binding ShowPageCanvas,Converter={StaticResource BoolVisibility}}">
       <Canvas Width="{Binding SelectedPage.Width}" Height="{Binding SelectedPage.Height}" Background="White">
        <ItemsControl ItemsSource="{Binding CurrentBlocks}">
         <ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
         <ItemsControl.ItemContainerStyle><Style TargetType="ContentPresenter"><Setter Property="Canvas.Left" Value="{Binding Bounds.X}"/><Setter Property="Canvas.Top" Value="{Binding Bounds.Y}"/></Style></ItemsControl.ItemContainerStyle>
         <ItemsControl.ItemTemplate><DataTemplate><Border Width="{Binding Bounds.Width}" Height="{Binding Bounds.Height}" Padding="1" Background="White" ClipToBounds="True">
          <Grid>
           <TextBlock Text="{Binding OriginalText}" FontSize="{Binding FontSize}" FontWeight="{Binding FontWeight}" Foreground="{Binding TextColor}" FontFamily="{Binding FontFamily}" TextWrapping="Wrap" VerticalAlignment="Center"><TextBlock.Visibility><MultiBinding Converter="{StaticResource DocumentPreviewVisibility}" ConverterParameter="original"><Binding Path="DataContext.ShowOriginal" RelativeSource="{RelativeSource AncestorType=UserControl}"/><Binding Path="HasTranslation"/></MultiBinding></TextBlock.Visibility></TextBlock>
           <TextBlock Text="{Binding TranslatedText}" FontSize="{Binding OverlayFontSize}" FontWeight="{Binding FontWeight}" Foreground="{StaticResource Text}" TextWrapping="Wrap" VerticalAlignment="Center"><TextBlock.Visibility><MultiBinding Converter="{StaticResource DocumentPreviewVisibility}"><Binding Path="DataContext.ShowChinese" RelativeSource="{RelativeSource AncestorType=UserControl}"/><Binding Path="HasTranslation"/></MultiBinding></TextBlock.Visibility></TextBlock>
          </Grid>
         </Border></DataTemplate></ItemsControl.ItemTemplate>
        </ItemsControl>
       </Canvas>
      </Viewbox>
     </Grid>
      <Border Grid.Row="2" BorderBrush="{StaticResource Line}" BorderThickness="0,1,0,0" Padding="2"><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><Button Content="上一页" Command="{Binding PreviousPageCommand}" Padding="6,2"/><TextBlock Text="{Binding PagePosition}" Margin="9,0" VerticalAlignment="Center"/><Button Content="下一页" Command="{Binding NextPageCommand}" Padding="6,2"/></StackPanel></Border>
    </Grid></Border>

   </Grid>

   <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="4" Margin="0,4,0,0"><Grid><StackPanel Orientation="Horizontal" VerticalAlignment="Center"><ComboBox Width="100" Padding="5,2" ItemsSource="{Binding ExportModeNames}" SelectedItem="{Binding SelectedExportModeName}" ToolTip="导出模式"/><Button Content="取消翻译" Command="{Binding CancelCommand}" Margin="4,0,0,0" Padding="6,3"/><Button Content="导出译文" Command="{Binding ExportCommand}" Margin="4,0,0,0" Padding="6,3"/><Button Content="移除文档" Command="{Binding RemoveCommand}" Style="{StaticResource DangerButton}" Margin="4,0,0,0" Padding="6,3"/></StackPanel><TextBlock Text="原文件不会被修改" Style="{StaticResource Caption}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid></Border>
  </Grid>
 </Grid>
</UserControl>
